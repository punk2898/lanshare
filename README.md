# 局域网共享

在一台电脑上打开，同一个 Wi-Fi 下的手机和电脑用浏览器就能**共享文字和文件**。不走外网，不用注册，其他人也不用装任何东西。

- 开共享的电脑页面上会显示**二维码**，别人扫码直接进入
- 自带 4 位**访问码**，同一 Wi-Fi 下的陌生人进不来（扫码会自动带上访问码）
- 文字和文件都保存在开共享那台电脑的 `~/LANShare` 里，关掉重开还在

## 用法一：Mac 客户端（推荐）

1. 到 [Releases](../../releases/latest) 下载 `局域网共享-mac.zip`，解压后把「局域网共享」拖进「应用程序」
2. 双击打开，浏览器会自动弹出共享页面，页面上有二维码和访问码
3. 其他人连同一个 Wi-Fi，扫码进入
4. 用完在页面上点「停止共享」

**第一次打开时可能会遇到这两个弹窗：**

- **「无法验证开发者」或「已损坏」**：这是因为 App 没有花钱做苹果签名。打开「系统设置 → 隐私与安全性」，拉到最下面点「仍要打开」。
  也可以在终端执行下面这行命令，之后就能正常双击：
  ```bash
  xattr -dr com.apple.quarantine /Applications/局域网共享.app
  ```
- **「是否允许接受传入的网络连接」**：点「允许」，不然别的设备连不上。

## 用法二：让 AI 帮你启动

把仓库克隆下来，用 Claude Code、Cursor 等 AI 编程工具打开这个文件夹，然后跟它说：

> 帮我启动局域网共享

AI 会按 [AGENTS.md](AGENTS.md) 里的步骤启动，然后告诉你地址和访问码。你的电脑上有没有装 Go 都行：没装的话，启动脚本会自动下载编译好的程序。

## 用法三：命令行

```bash
./scripts/start.sh
```

终端里会打印地址、访问码和一个二维码。常用参数：

| 参数 | 作用 |
|---|---|
| `-password 1234` | 指定访问码（会记住） |
| `-no-password` | 这次不设访问码 |
| `-port 9000` | 换端口（默认 8000，被占用会自动往后找） |
| `-dir 路径` | 换数据目录（默认 `~/LANShare`） |
| `-no-open` | 不自动打开浏览器 |

Windows 用户可以从 Releases 下载 `lanshare-windows-amd64.exe`，双击运行。

## 常见问题

- **别人打不开？** 确认连的是同一个 Wi-Fi。有些公司网络和酒店 Wi-Fi 会禁止设备之间互相访问，这种情况可以改用手机热点。
- **手机上「复制全部」不管用？** 因为是 http 页面，部分手机浏览器不允许网页写剪贴板，长按文本框手动复制就行。
- **安全吗？** 数据只在局域网里传，不经过任何服务器。访问码只能挡住随手乱连的人，别在公共 Wi-Fi 上共享敏感内容。

## 开发

```bash
go run ./cmd/lanshare        # 本地运行
./scripts/build-mac.sh       # 打包 Mac 客户端到 dist/
git tag v1.0.0 && git push --tags   # 触发 GitHub Actions，自动发布到 Releases
```

代码结构见 [AGENTS.md](AGENTS.md)。
